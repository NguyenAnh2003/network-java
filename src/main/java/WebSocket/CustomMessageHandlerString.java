package WebSocket;

import javax.websocket.MessageHandler;

public class CustomMessageHandlerString implements MessageHandler.Whole<String> {
    @Override
    public void onMessage(String s) {

    }
}
